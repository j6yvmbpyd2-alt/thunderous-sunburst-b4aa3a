(()=>{
  const WATCH_KEY='mtgWatchV2',HISTORY_KEY='mtgHistoryV2';
  const moneyText=s=>Number(String(s||'').replace(/[^0-9.]/g,''))||0;
  const load=(k)=>{try{return JSON.parse(localStorage.getItem(k)||'[]')}catch{return[]}};
  const save=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
  function exactMeta(card){
    const h2=card.querySelector('h2');if(!h2)return null;
    const name=card.dataset.cardName||h2.textContent.trim(),setName=card.dataset.setName||'',setCode=card.dataset.setCode||'',collector=card.dataset.collector||'',id=card.dataset.cardId||'',finish=card.dataset.finish||'nonfoil';
    const current=moneyText(card.querySelector('.price')?.textContent),maxBox=[...card.querySelectorAll('.kpi')].find(x=>/max buy/i.test(x.textContent||'')),target=moneyText(maxBox?.querySelector('b')?.textContent||'');
    if(id)return{id,name,setName,setCode,collector,finish,current,target};
    const muted=h2.parentElement?.querySelector('.muted')?.textContent||'',m=muted.match(/^(.*?)\s*•\s*#(.+)$/);if(!m)return null;
    return{name,setName:m[1].trim(),collector:m[2].trim(),finish,current,target};
  }
  async function resolveExact(meta){
    if(meta.id){try{const r=await fetch(`https://api.scryfall.com/cards/${encodeURIComponent(meta.id)}`,{cache:'no-store'});if(r.ok)return await r.json()}catch{}}
    if(meta.setCode&&meta.collector){try{const r=await fetch(`https://api.scryfall.com/cards/${encodeURIComponent(meta.setCode.toLowerCase())}/${encodeURIComponent(meta.collector)}`,{cache:'no-store'});if(r.ok)return await r.json()}catch{}}
    try{const r=await fetch(`https://api.scryfall.com/cards/search?q=${encodeURIComponent(`!"${meta.name}"`)}`,{cache:'no-store'});if(r.ok){const d=await r.json();return(d.data||[]).find(c=>String(c.collector_number)===String(meta.collector)&&(!meta.setName||String(c.set_name).toLowerCase()===String(meta.setName).toLowerCase()))||null}}catch{}return null;
  }
  function addHistory(id,p){if(!p)return;const h=load(HISTORY_KEY);h.push({id,p:+p,t:Date.now()});save(HISTORY_KEY,h.slice(-1000))}
  function refreshExistingUI(){try{if(typeof window.renderWatch==='function')window.renderWatch()}catch{}}
  async function add(card,btn,status){const meta=exactMeta(card);if(!meta){status.textContent='Could not read exact printing from this decision.';status.className='bad';return}btn.disabled=true;btn.textContent='Adding…';status.textContent='Resolving exact printing…';status.className='muted';try{const exact=await resolveExact(meta);if(!exact)throw Error('Exact Scryfall printing could not be resolved.');const a=load(WATCH_KEY),idx=a.findIndex(x=>x.id===exact.id&&String(x.finish||'nonfoil')===String(meta.finish||'nonfoil')),row={id:exact.id,name:exact.name,set:exact.set_name,num:exact.collector_number,finish:meta.finish||'nonfoil',target:meta.target||'',last:meta.current||'',checked:Date.now()};if(idx>=0){a[idx]={...a[idx],...row};status.textContent=`Watch updated • target ${meta.target?'$'+meta.target.toFixed(2):'not set'}.`;btn.textContent='Watchlist Updated'}else{a.unshift(row);if(a.length>75)a.length=75;status.textContent=`Added to Watchlist • target ${meta.target?'$'+meta.target.toFixed(2):'not set'}.`;btn.textContent='Added to Watchlist'}save(WATCH_KEY,a);addHistory(exact.id,meta.current);refreshExistingUI();status.className='good'}catch(e){status.textContent=e.message||'Could not add watch.';status.className='bad';btn.textContent='Add to Watchlist'}finally{btn.disabled=false}}
  function enhance(){const card=document.getElementById('intelCard');if(!card||!card.querySelector('h2'))return;let host=document.getElementById('decisionWatchActions');if(host)return;host=document.createElement('div');host.id='decisionWatchActions';host.className='notice';host.style.marginTop='12px';host.innerHTML='<button id="decisionAddWatch" class="secondary small" style="width:auto;margin:0">Add to Watchlist</button><button id="decisionGoWatch" class="ghost small" style="width:auto;margin:0 0 0 8px">Open Watchlist</button><div id="decisionWatchStatus" class="muted" style="margin-top:7px"></div>';const anchor=[...card.querySelectorAll('h2')].find(x=>/why this call/i.test(x.textContent||''));if(anchor)card.insertBefore(host,anchor);else card.appendChild(host);const meta=exactMeta(card),existing=meta?load(WATCH_KEY).find(x=>(meta.id?x.id===meta.id:x.name===meta.name&&String(x.num)===String(meta.collector))&&String(x.finish||'nonfoil')===String(meta.finish||'nonfoil')):null;if(existing){document.getElementById('decisionAddWatch').textContent='Update Watchlist Target';document.getElementById('decisionWatchStatus').textContent=`Already watched${existing.target?` • current target $${Number(existing.target).toFixed(2)}`:''}.`}document.getElementById('decisionAddWatch').onclick=()=>add(card,document.getElementById('decisionAddWatch'),document.getElementById('decisionWatchStatus'));document.getElementById('decisionGoWatch').onclick=()=>{document.getElementById('intelModal').style.display='none';document.querySelector('[data-tab="watchlist"]')?.click();refreshExistingUI()}}
  const obs=new MutationObserver(()=>enhance());function init(){obs.observe(document.body,{childList:true,subtree:true});enhance()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();