(()=>{
  const money=n=>Number(n||0).toLocaleString(undefined,{style:'currency',currency:'USD'});
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const bust=u=>u+(u.includes('?')?'&':'?')+'_='+Date.now();
  function svgChart(points){const vals=points.map(x=>Number(x.price)).filter(x=>x>0);if(vals.length<2)return'<div class="empty">More price snapshots are needed for a chart.</div>';const w=620,h=190,pad=18,min=Math.min(...vals),max=Math.max(...vals),span=Math.max(.01,max-min),coords=vals.map((v,i)=>`${pad+(i/(vals.length-1))*(w-pad*2)},${h-pad-((v-min)/span)*(h-pad*2)}`).join(' ');return`<svg viewBox="0 0 ${w} ${h}" style="width:100%;height:auto;background:#0b1424;border:1px solid #26364f;border-radius:12px"><polyline points="${coords}" fill="none" stroke="currentColor" stroke-width="3"/><text x="${pad}" y="14" fill="currentColor" font-size="11">${money(max)}</text><text x="${pad}" y="${h-4}" fill="currentColor" font-size="11">${money(min)}</text></svg>`}
  function ensureModal(){if(document.getElementById('intelModal'))return;const el=document.createElement('div');el.id='intelModal';el.style='display:none;position:fixed;inset:0;z-index:50;background:rgba(0,0,0,.72);padding:18px;overflow:auto';el.innerHTML='<div id="intelCard" class="card" style="max-width:760px;margin:30px auto"></div>';document.body.appendChild(el);el.addEventListener('click',e=>{if(e.target===el)el.style.display='none'})}
  async function openIntel(id){ensureModal();const modal=document.getElementById('intelModal'),card=document.getElementById('intelCard');modal.style.display='block';card.innerHTML='<div class="notice">Loading card intelligence…</div>';try{const d=await fetch(bust('/.netlify/functions/card-intelligence?id='+encodeURIComponent(id)),{cache:'no-store'}).then(r=>r.json());if(!d.ok)throw Error(d.error||'Unable to load intelligence');const x=d.card,i=d.intelligence,h=d.history||[],cls=i.decision==='BUY'?'good':i.decision==='PASS'?'bad':'watch',consCls=Number(i.pricing_consistency)>=80?'good':Number(i.pricing_consistency)>=65?'watch':'bad',baseCls=Number(i.baseline_quality)>=80?'good':Number(i.baseline_quality)>=65?'watch':'bad',histReady=Number(i.historical_points||0)>0;card.innerHTML=`<div class="row"><div><h2>${esc(x.name)}</h2><div class="muted">${esc(x.set_name||x.set)} • #${esc(x.collector_number||'')}</div></div><button id="intelClose" class="secondary" style="width:auto;margin:0">Close</button></div><div style="display:grid;grid-template-columns:${x.image?'130px 1fr':'1fr'};gap:14px;margin-top:12px">${x.image?`<img src="${esc(x.image)}" style="width:130px;border-radius:10px">`:''}<div><div class="price">${money(i.current)} <span class="tiny">live</span></div><span class="pill ${cls}"><b>${esc(i.decision)}</b></span><span class="pill">Buy Now ${i.buy_confidence}%</span><span class="pill">${esc(i.trend)}</span><div class="grid2" style="margin-top:10px"><div class="kpi"><span class="muted">Live price</span><b>${money(i.current)}</b></div><div class="kpi"><span class="muted">MTGJSON latest</span><b>${i.historical_current?money(i.historical_current):'—'}</b></div></div><div style="margin-top:7px"><span class="pill ${consCls}">Pricing consistency ${Number(i.pricing_consistency||0)}%</span><span class="pill ${baseCls}">Baseline quality ${Number(i.baseline_quality||0)}%</span>${i.source_gap_pct!=null?`<span class="pill">source gap ${Number(i.source_gap_pct).toFixed(1)}%</span>`:''}</div><div class="grid2" style="margin-top:10px"><div class="kpi"><span class="muted">Fair buy</span><b>${money(i.fair_buy)}</b></div><div class="kpi"><span class="muted">Strong buy</span><b>${money(i.strong_buy)}</b></div></div><div class="muted" style="margin-top:9px">Historical price percentile: ${i.percentile==null?'—':i.percentile+'%'}</div><div style="margin-top:7px"><span class="pill">Historical ${Number(i.historical_confidence||0)}%</span><span class="pill">Live ${Number(i.live_confidence||0)}%</span></div><div class="tiny" style="margin-top:5px">${histReady?`${i.historical_points} daily historical points + ${i.live_points} live hourly points`:`Historical cache not seeded yet • ${i.live_points} live hourly points`}</div></div></div><div style="margin-top:14px"><h2>Price history</h2>${svgChart(h)}</div><div class="grid3" style="margin-top:12px"><div class="kpi"><span class="muted">30d avg</span><b>${i.avg30?money(i.avg30):'—'}</b></div><div class="kpi"><span class="muted">30d median</span><b>${i.median30?money(i.median30):'—'}</b></div><div class="kpi"><span class="muted">30d low</span><b>${i.low30?money(i.low30):'—'}</b></div></div><div class="grid3" style="margin-top:8px"><div class="kpi"><span class="muted">90d avg</span><b>${i.avg90?money(i.avg90):'—'}</b></div><div class="kpi"><span class="muted">90d median</span><b>${i.median90?money(i.median90):'—'}</b></div><div class="kpi"><span class="muted">90d low</span><b>${i.low90?money(i.low90):'—'}</b></div></div><div class="grid3" style="margin-top:8px"><div class="kpi"><span class="muted">24h avg</span><b>${i.avg24?money(i.avg24):'—'}</b></div><div class="kpi"><span class="muted">24h low</span><b>${i.low24?money(i.low24):'—'}</b></div><div class="kpi"><span class="muted">24h high</span><b>${i.high24?money(i.high24):'—'}</b></div></div>${i.regime_shift?`<div class="bad" style="margin-top:12px">Structural repricing detected (${Number(i.regime_delta||0).toFixed(1)}% shift). Older historical prices are being heavily down-weighted and high-confidence BUY is blocked.</div>`:''}${Number(i.baseline_quality)<65&&!i.regime_shift?'<div class="bad" style="margin-top:12px">Historical baseline quality is weak because the 90-day series contains too many outliers. Confidence is capped.</div>':''}${Number(i.pricing_consistency)<65?'<div class="bad" style="margin-top:12px">Historical and live price sources disagree too much for a high-confidence BUY. Historical evidence is being down-weighted.</div>':''}<div class="notice" style="margin-top:12px">${esc(x.reason||'')}</div>${i.history_source?`<div class="tiny" style="margin-top:8px">Historical source: ${esc(i.history_source.source||'MTGJSON')} • ${esc(i.history_source.provider||'TCGplayer')} • ${esc(i.history_source.list||'retail')} • ${esc(i.history_source.finish||'normal')} • ${esc(i.history_source.resolution||'daily')}</div>`:''}${x.scryfall_uri?`<a href="${esc(x.scryfall_uri)}" style="display:block;margin-top:10px;color:#93c5fd">View exact printing</a>`:''}`;document.getElementById('intelClose').onclick=()=>modal.style.display='none'}catch(e){card.innerHTML=`<div class="bad">${esc(e.message)}</div><button id="intelClose" class="secondary">Close</button>`;document.getElementById('intelClose').onclick=()=>modal.style.display='none'}}
  async function enhance(){const box=document.getElementById('top20Box');if(!box)return;try{const d=await fetch(bust('/.netlify/functions/top20'),{cache:'no-store'}).then(r=>r.json()),items=[...box.querySelectorAll('.item')];(d.top20||[]).forEach((x,i)=>{const item=items[i];if(!item||item.querySelector('.intel-btn'))return;const b=document.createElement('button');b.className='secondary small intel-btn';b.style='width:auto;margin-top:8px';b.textContent='Card Intelligence';b.onclick=()=>openIntel(x.id);item.appendChild(b)})}catch{}}
  const obs=new MutationObserver(()=>enhance());if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{ensureModal();const box=document.getElementById('top20Box');if(box)obs.observe(box,{childList:true});enhance()});else{ensureModal();const box=document.getElementById('top20Box');if(box)obs.observe(box,{childList:true});enhance()}
})();
