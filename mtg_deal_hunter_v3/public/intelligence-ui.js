(()=>{const money=n=>Number(n||0).toLocaleString(undefined,{style:'currency',currency:'USD'}),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c])),bust=u=>u+(u.includes('?')?'&':'?')+'_='+Date.now();
function holdings(id){try{const inv=JSON.parse(localStorage.getItem('mtgDragonShieldInvestmentsV1')||'[]');const rows=inv.filter(x=>x.id===id),qty=rows.reduce((s,x)=>s+(Number(x.qty)||1),0),value=rows.reduce((s,x)=>s+(Number(x.current)||0)*(Number(x.qty)||1),0);return{qty,value}}catch{return{qty:0,value:0}}}
function chart(p){const v=p.map(x=>Number(x.price)).filter(x=>x>0);if(v.length<2)return'<div class="empty">More price snapshots are needed.</div>';const w=620,h=170,pad=18,min=Math.min(...v),max=Math.max(...v),span=Math.max(.01,max-min),pts=v.map((x,i)=>`${pad+i/(v.length-1)*(w-pad*2)},${h-pad-(x-min)/span*(h-pad*2)}`).join(' ');return`<svg viewBox="0 0 ${w} ${h}" style="width:100%;height:auto;background:#0b1424;border:1px solid #26364f;border-radius:12px"><polyline points="${pts}" fill="none" stroke="currentColor" stroke-width="3"/></svg>`}
function modal(){if(document.getElementById('intelModal'))return;const e=document.createElement('div');e.id='intelModal';e.style='display:none;position:fixed;inset:0;z-index:50;background:rgba(0,0,0,.72);padding:18px;overflow:auto';e.innerHTML='<div id="intelCard" class="card" style="max-width:760px;margin:30px auto"></div>';document.body.appendChild(e);e.onclick=x=>{if(x.target===e)e.style.display='none'}}
async function openIntel(id){modal();const m=document.getElementById('intelModal'),c=document.getElementById('intelCard');m.style.display='block';c.innerHTML='<div class="notice">Building purchase decision…</div>';try{const d=await fetch(bust('/.netlify/functions/card-intelligence?id='+encodeURIComponent(id)),{cache:'no-store'}).then(r=>r.json());if(!d.ok)throw Error(d.error);const x=d.card,i=d.intelligence,h=holdings(id),overlap=h.qty>=8?'HIGH':h.qty>=4?'MEDIUM':'LOW',suggested=overlap==='HIGH'?{min:0,max:0,label:'Portfolio concentration: no additional position'}:overlap==='MEDIUM'?{min:Math.min(10,i.position.min),max:Math.min(15,i.position.max),label:'Reduced for existing exposure'}:i.position,cls=i.decision==='BUY'?'good':i.decision==='PASS'?'bad':'watch',gcls=['A','B'].includes(i.evidence_grade)?'good':i.evidence_grade==='C'?'watch':'bad';c.innerHTML=`<div class="row"><div><h2>${esc(x.name)}</h2><div class="muted">${esc(x.set_name||x.set)} • #${esc(x.collector_number||'')}</div></div><button id="intelClose" class="secondary" style="width:auto">Close</button></div><div style="display:grid;grid-template-columns:${x.image?'130px 1fr':'1fr'};gap:14px;margin-top:12px">${x.image?`<img src="${esc(x.image)}" style="width:130px;border-radius:10px">`:''}<div><div class="price">${money(i.current)}</div><span class="pill ${cls}"><b>${i.decision}</b></span><span class="pill">${i.buy_confidence}% confidence</span><span class="pill ${gcls}"><b>Evidence ${i.evidence_grade}</b> • ${i.evidence_score}</span><div class="grid3" style="margin-top:10px"><div class="kpi"><span class="muted">Max buy</span><b>${money(i.max_buy)}</b></div><div class="kpi"><span class="muted">Liquidity</span><b>${i.liquidity}</b></div><div class="kpi"><span class="muted">Model risk</span><b>${i.risk}</b></div></div><div class="notice" style="margin-top:10px"><b>Suggested position:</b> ${suggested.max?`${money(suggested.min)}–${money(suggested.max)}`:'$0'} • ${esc(suggested.label)}<br><span class="tiny">Investment holdings: ${h.qty} copies • ${money(h.value)} current exposure • concentration ${overlap}</span></div></div></div><h2 style="margin-top:14px">Why this call</h2>${(i.why||[]).map(v=>`<div class="good" style="margin-top:6px">✓ ${esc(v)}</div>`).join('')}<h2 style="margin-top:14px">Risks / missing evidence</h2>${(i.risks||[]).map(v=>`<div class="${i.risk==='LOW'?'notice':'bad'}" style="margin-top:6px">• ${esc(v)}</div>`).join('')}<div class="grid3" style="margin-top:14px"><div class="kpi"><span class="muted">Live / MTGJSON</span><b>${money(i.current)} / ${i.historical_current?money(i.historical_current):'—'}</b></div><div class="kpi"><span class="muted">30d median</span><b>${i.median30?money(i.median30):'—'}</b></div><div class="kpi"><span class="muted">90d median</span><b>${i.median90?money(i.median90):'—'}</b></div></div><div style="margin-top:8px"><span class="pill">Consistency ${i.pricing_consistency}%</span><span class="pill">Baseline ${i.baseline_quality}%</span><span class="pill">90d percentile ${i.percentile==null?'—':i.percentile+'%'}</span><span class="pill">Historical ${i.historical_confidence}%</span><span class="pill">Live ${i.live_confidence}%</span></div><h2 style="margin-top:14px">Price history</h2>${chart(d.history||[])}<div class="tiny" style="margin-top:8px">Evidence grade measures data quality, not expected profit. Position sizing is a risk-control suggestion, not a guarantee. Transaction-volume and complete reprint-risk evidence are not yet available.</div>${x.scryfall_uri?`<a href="${esc(x.scryfall_uri)}" style="display:block;margin-top:10px;color:#93c5fd">View exact printing</a>`:''}`;document.getElementById('intelClose').onclick=()=>m.style.display='none'}catch(e){c.innerHTML=`<div class="bad">${esc(e.message||'Unable to load')}</div>`}}
async function enhance(){const box=document.getElementById('top20Box');if(!box)return;try{const d=await fetch(bust('/.netlify/functions/top20'),{cache:'no-store'}).then(r=>r.json()),items=[...box.querySelectorAll('.item')];(d.top20||[]).forEach((x,i)=>{const it=items[i];if(!it||it.querySelector('.intel-btn'))return;const b=document.createElement('button');b.className='secondary small intel-btn';b.style='width:auto;margin-top:8px';b.textContent='Purchase Decision';b.onclick=()=>openIntel(x.id);it.appendChild(b)})}catch{}}
const o=new MutationObserver(enhance);function init(){modal();const b=document.getElementById('top20Box');if(b)o.observe(b,{childList:true});enhance()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();