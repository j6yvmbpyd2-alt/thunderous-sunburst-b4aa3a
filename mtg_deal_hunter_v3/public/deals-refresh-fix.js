(()=>{
  const bust=u=>u+(u.includes('?')?'&':'?')+'_='+Date.now();
  async function loadFeed(){try{const r=await fetch(bust('/.netlify/functions/deals'),{cache:'no-store'});return await r.json()}catch{return null}}
  function ensurePanel(){let p=document.getElementById('marketSweepTelemetry');if(p)return p;const status=document.getElementById('dealStatus');if(!status)return null;p=document.createElement('div');p.id='marketSweepTelemetry';p.className='card';p.style='margin-top:12px;padding:12px';status.insertAdjacentElement('afterend',p);return p}
  function metrics(d){const sources=Number(d?.source_count||0),healthy=Number(d?.healthy_sources||0),seen=Number(d?.items_seen||0),usable=Number(d?.items_usable||0),evald=Number(d?.items_evaluated||0),candidates=Number(d?.candidate_only_count||0),qualified=Number(d?.qualified_this_run||0),watchHits=Number(d?.watch_hits||0),retained=Array.isArray(d?.deals)?d.deals.length:Number(d?.retained_deals||0),at=d?.updated_at?new Date(d.updated_at).toLocaleString():'never';return{sources,healthy,seen,usable,evald,candidates,qualified,watchHits,retained,at}}
  function paint(d){const p=ensurePanel();if(!p)return;const m=metrics(d||{}),configured=m.sources>0,active=m.healthy>0;const sourceState=!configured?'NO MARKET SOURCES CONFIGURED':active?`${m.healthy}/${m.sources} market sources healthy`:`${m.sources} source${m.sources===1?'':'s'} configured • NONE HEALTHY`;p.innerHTML=`<div class="row"><div><b>Market Sweep Telemetry</b><div class="${active?'good':'bad'}">${sourceState}</div></div><span class="pill ${active?'good':'bad'}">${active?'SCANNER ACTIVE':'SCANNER NEEDS ATTENTION'}</span></div><div class="grid3" style="margin-top:10px"><div class="kpi"><span class="muted">Configured / healthy</span><b>${m.sources} / ${m.healthy}</b></div><div class="kpi"><span class="muted">Store products seen</span><b>${m.seen}</b></div><div class="kpi"><span class="muted">Exact listings usable</span><b>${m.usable}</b></div></div><div class="grid3" style="margin-top:9px"><div class="kpi"><span class="muted">Listings evaluated</span><b>${m.evald}</b></div><div class="kpi"><span class="muted">Store-sale candidates</span><b>${m.candidates}</b></div><div class="kpi"><span class="muted">New verified deals</span><b>${m.qualified}</b></div></div><div class="grid3" style="margin-top:9px"><div class="kpi"><span class="muted">Price-watch hits</span><b>${m.watchHits}</b></div><div class="kpi"><span class="muted">Retained feed entries</span><b>${m.retained}</b></div><div class="kpi"><span class="muted">Last sweep</span><b style="font-size:12px">${m.at}</b></div></div><div class="tiny" style="margin-top:9px">Store-sale candidates are intentionally not called verified deals unless the scanner can match them to defensible reference pricing.</div>`;return m}
  async function refreshTelemetry(){const d=await loadFeed();if(d)paint(d);return d}
  function init(){const btn=document.getElementById('refreshDeals');if(!btn)return;btn.dataset.previewScanFix='5';btn.onclick=async()=>{if(btn.disabled)return;const old=btn.textContent;btn.disabled=true;btn.textContent='Scanning…';const status=document.getElementById('dealStatus');if(status){status.textContent='Running fresh market sweep and price-watch scan…';status.className='notice'}const p=ensurePanel();if(p)p.innerHTML='<div class="notice">Running market sweep now…</div>';try{const [dealRes,watchRes]=await Promise.all([fetch(bust('/.netlify/functions/scan-deals-now'),{cache:'no-store'}),fetch(bust('/.netlify/functions/price-watch-now'),{cache:'no-store'})]);const dealData=await dealRes.json().catch(()=>({})),watchData=await watchRes.json().catch(()=>({}));if(!dealRes.ok||dealData.ok===false)throw Error(dealData.error||`Deal scan returned ${dealRes.status}`);if(!watchRes.ok||watchData.ok===false)throw Error(watchData.error||`Price-watch scan returned ${watchRes.status}`);try{if(typeof window.loadDeals==='function')await window.loadDeals()}catch{}const feed=await refreshTelemetry()||dealData,m=paint(feed);if(status){status.textContent=m?.healthy>0?`Market sweep complete • ${m.qualified} new verified deal${m.qualified===1?'':'s'} • ${m.candidates} store-sale candidate${m.candidates===1?'':'s'} • ${m.watchHits} price-watch hit${m.watchHits===1?'':'s'}`:'Market sweep complete • no healthy market sources';status.className=m?.healthy>0?'notice':'bad'}}catch(e){if(status){status.textContent='Deal refresh failed: '+e.message;status.className='bad'}if(p)p.innerHTML=`<div class="bad">Market sweep failed: ${String(e.message||e)}</div>`}finally{btn.disabled=false;btn.textContent=old}};refreshTelemetry();const obs=new MutationObserver(()=>{setTimeout(()=>refreshTelemetry(),0)}),status=document.getElementById('dealStatus');if(status)obs.observe(status,{childList:true,subtree:true,characterData:true})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();