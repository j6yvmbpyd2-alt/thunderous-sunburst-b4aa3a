(()=>{
  const KEY='mtgWatchV2',money=n=>Number(n||0).toLocaleString(undefined,{style:'currency',currency:'USD'}),load=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}},save=v=>localStorage.setItem(KEY,JSON.stringify(v)),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const cache=new Map();
  async function exact(id){if(cache.has(id))return cache.get(id);try{const r=await fetch(`https://api.scryfall.com/cards/${encodeURIComponent(id)}`,{cache:'no-store'});if(!r.ok)return null;const c=await r.json();cache.set(id,c);return c}catch{return null}}
  function pick(c,f){const p=c?.prices||{};if(f==='foil')return Number(p.usd_foil)||null;if(f==='etched')return Number(p.usd_etched)||null;return Number(p.usd)||Number(p.usd_foil)||Number(p.usd_etched)||null}
  async function render(){const box=document.getElementById('watchlistBox');if(!box)return;const rows=load();if(!rows.length){box.innerHTML='<div class="empty">No watched cards yet.</div>';return}box.innerHTML='<div class="notice">Loading exact printing images…</div>';const cards=await Promise.all(rows.map(x=>exact(x.id)));box.innerHTML=rows.map((x,i)=>{const c=cards[i],img=c?.image_uris?.normal||c?.card_faces?.[0]?.image_uris?.normal||'',p=pick(c,x.finish)||Number(x.last)||0,target=Number(x.target)||0,hit=target&&p&&p<=target;return`<div class="item" data-watch-index="${i}"><div style="display:grid;grid-template-columns:${img?'92px 1fr':'1fr'};gap:12px;align-items:start">${img?`<img src="${esc(img)}" style="width:92px;border-radius:10px">`:''}<div><div class="row"><div><b>${esc(x.name)}</b><div class="muted">${esc(x.set)} • #${esc(x.num)} • ${esc(x.finish||'nonfoil')}</div></div>${p?`<b>${money(p)}</b>`:''}</div><div style="margin-top:6px"><span class="pill ${hit?'good':''}">Target ${target?money(target):'—'}</span>${hit?'<span class="pill good">TARGET HIT</span>':''}</div><div class="buttons"><button class="secondary small watch-decision" data-id="${esc(x.id)}" data-finish="${esc(x.finish||'any')}">Purchase Decision</button><button class="ghost small watch-remove" data-index="${i}">Remove</button></div></div></div></div>`}).join('');box.querySelectorAll('.watch-decision').forEach(b=>b.onclick=()=>window.openPurchaseDecision?.(b.dataset.id,b.dataset.finish||'any'));box.querySelectorAll('.watch-remove').forEach(b=>b.onclick=()=>{const a=load();a.splice(Number(b.dataset.index),1);save(a);render()})}
  const obs=new MutationObserver(()=>{const p=document.getElementById('watchlist');if(p?.classList.contains('active'))render()});
  function init(){const p=document.getElementById('watchlist');if(!p)return;obs.observe(p,{attributes:true,attributeFilter:['class']});document.querySelector('[data-tab="watchlist"]')?.addEventListener('click',()=>setTimeout(render,30));render()}
  window.renderEnhancedWatchlist=render;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();