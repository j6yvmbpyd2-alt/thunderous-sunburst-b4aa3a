(()=>{
  const KEY='mtgWatchV2',HISTORY='mtgHistoryV2',money=n=>Number(n||0).toLocaleString(undefined,{style:'currency',currency:'USD'}),load=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}},save=v=>localStorage.setItem(KEY,JSON.stringify(v)),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const cardCache=new Map(),decisionCache=new Map();let refreshing=false;
  async function exact(id,force=false){if(!force&&cardCache.has(id))return cardCache.get(id);try{const r=await fetch(`https://api.scryfall.com/cards/${encodeURIComponent(id)}?_=${Date.now()}`,{cache:'no-store'});if(!r.ok)return null;const c=await r.json();cardCache.set(id,c);return c}catch{return null}}
  async function decision(id,finish){const k=id+'|'+finish;if(decisionCache.has(k))return decisionCache.get(k);try{const r=await fetch(`/.netlify/functions/card-intelligence-safe?id=${encodeURIComponent(id)}&finish=${encodeURIComponent(finish||'any')}&_=${Date.now()}`,{cache:'no-store'}),d=await r.json();const v=r.ok&&d.ok?d:null;decisionCache.set(k,v);return v}catch{return null}}
  function pick(c,f){const p=c?.prices||{};if(f==='foil')return Number(p.usd_foil)||null;if(f==='etched')return Number(p.usd_etched)||null;return Number(p.usd)||Number(p.usd_foil)||Number(p.usd_etched)||null}
  function record(id,p){if(!(p>0))return;try{const h=JSON.parse(localStorage.getItem(HISTORY)||'[]');h.push({id,p:+p,t:Date.now()});localStorage.setItem(HISTORY,JSON.stringify(h.slice(-1000)))}catch{}}
  async function refreshAllEnhanced(){if(refreshing)return;refreshing=true;const btn=document.getElementById('refreshAll'),rows=load();if(btn){btn.disabled=true;btn.textContent='Refreshing…'}try{const updated=[...rows];cardCache.clear();decisionCache.clear();for(let i=0;i<rows.length;i++){if(btn)btn.textContent=`Refreshing ${i+1}/${rows.length}`;const c=await exact(rows[i].id,true);if(c){const p=pick(c,rows[i].finish);updated[i]={...rows[i],last:p||rows[i].last,checked:Date.now()};record(rows[i].id,p)}if(i<rows.length-1)await new Promise(r=>setTimeout(r,90))}save(updated);await render()}finally{refreshing=false;if(btn){btn.disabled=false;btn.textContent='Refresh all'}}}
  async function render(){const box=document.getElementById('watchlistBox');if(!box)return;const rows=load();if(!rows.length){box.innerHTML='<div class="tiny good">Enhanced Watchlist v2 loaded</div><div class="empty">No watched cards yet.</div>';return}box.innerHTML='<div class="tiny good">Enhanced Watchlist v2 loaded</div><div class="notice" style="margin-top:8px">Loading exact printing images and intelligence summaries…</div>';const cards=await Promise.all(rows.map(x=>exact(x.id)));box.innerHTML='<div class="tiny good">Enhanced Watchlist v2 loaded</div>'+rows.map((x,i)=>{const c=cards[i],img=c?.image_uris?.normal||c?.card_faces?.[0]?.image_uris?.normal||'',p=pick(c,x.finish)||Number(x.last)||0,target=Number(x.target)||0,hit=target&&p&&p<=target;return`<div class="item" data-watch-index="${i}"><div style="display:grid;grid-template-columns:${img?'96px 1fr':'1fr'};gap:12px;align-items:start">${img?`<img src="${esc(img)}" style="width:96px;border-radius:10px;display:block">`:''}<div><div class="row"><div><b>${esc(x.name)}</b><div class="muted">${esc(x.set)} • #${esc(x.num)} • ${esc(x.finish||'nonfoil')}</div></div>${p?`<b>${money(p)}</b>`:''}</div><div style="margin-top:6px"><span class="pill ${hit?'good':''}">Target ${target?money(target):'—'}</span>${hit?'<span class="pill good">TARGET HIT</span>':''}</div><div id="watchIntel${i}" class="notice" style="margin-top:8px">Loading Purchase Decision…</div><div class="buttons"><button class="secondary small watch-server" data-index="${i}">Add Server Watch</button><button class="secondary small watch-decision" data-id="${esc(x.id)}" data-finish="${esc(x.finish||'any')}">Full Purchase Decision</button><button class="ghost small watch-remove" data-index="${i}">Remove</button></div></div></div></div>`}).join('');box.querySelectorAll('.watch-server').forEach(b=>b.onclick=async()=>{const i=Number(b.dataset.index),x=rows[i],c=cards[i];if(!x||!c)return;await window.addOrUpdateServerWatch?.({name:x.name,set:c.set,collector_number:c.collector_number,finish:x.finish||'nonfoil',price:pick(c,x.finish),target:x.target},{suggestedTarget:Number(x.target)||pick(c,x.finish),button:b})});box.querySelectorAll('.watch-decision').forEach(b=>b.onclick=()=>window.openPurchaseDecision?.(b.dataset.id,b.dataset.finish||'any'));box.querySelectorAll('.watch-remove').forEach(b=>b.onclick=()=>{const a=load();a.splice(Number(b.dataset.index),1);save(a);decisionCache.clear();render()});rows.forEach(async(x,i)=>{const host=document.getElementById('watchIntel'+i);if(!host)return;const d=await decision(x.id,x.finish||'any');if(!d){host.textContent='Purchase Decision unavailable for this printing.';host.className='bad';return}const intel=d.intelligence,cl=intel.decision==='BUY'?'good':intel.decision==='PASS'?'bad':'watch';host.className=cl;host.innerHTML=`<b>${esc(intel.decision)}</b> • ${Number(intel.buy_confidence||0)}% confidence • Data Quality ${esc(intel.data_quality_grade||intel.evidence_grade||'—')}<br><span class="tiny">Max buy ${intel.max_buy?money(intel.max_buy):'—'} • Reprint risk ${esc(intel.reprint_risk||'UNKNOWN')} • ${esc(intel.entry_trigger||'')}</span>`})}
  function bindRefresh(){const b=document.getElementById('refreshAll');if(!b)return;b.onclick=refreshAllEnhanced}function takeControl(){window.renderWatch=render;window.renderEnhancedWatchlist=render;window.refreshAll=refreshAllEnhanced;bindRefresh()}const obs=new MutationObserver(()=>{const p=document.getElementById('watchlist');if(p?.classList.contains('active')){takeControl();setTimeout(render,0)}});function init(){const p=document.getElementById('watchlist');if(!p)return;takeControl();obs.observe(p,{attributes:true,attributeFilter:['class']});document.querySelector('[data-tab="watchlist"]')?.addEventListener('click',()=>{takeControl();setTimeout(render,10)});setTimeout(()=>{takeControl();render()},50);setTimeout(takeControl,500)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();